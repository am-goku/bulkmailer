import { createSlice } from "@reduxjs/toolkit";

const initialState = {
    emailGroup: [],
    
}

export const emailSlice = createSlice({
    name: 'emailData',
    initialState,
    reducers: {

    }
})