export const sendMailController = async (req, res) => {
    try {
        // TODO: Call for helpers and response accordingly
        res.status(200).send(true)
    } catch (error) {
        // TODO: Error management
    }
}